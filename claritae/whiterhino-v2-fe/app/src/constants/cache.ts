import CONFIG from "../config";

export const AppCachedKeys = {
  AppLanguage: `${CONFIG.USER_TYPE}-AppLanguage`,
  Version: `${CONFIG.USER_TYPE}-Version`,
  MaintenanceAlert: `${CONFIG.USER_TYPE}-MaintenanceAlert`,
};
